name: executive_system
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.7.0
  firebase_messaging: ^16.2.0
  http: ^1.1.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  table_calendar: ^3.1.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Indentation: flutter (0) -> assets (2 spaces) -> - (4 spaces)
  assets:
    - assets/portal_logo.png